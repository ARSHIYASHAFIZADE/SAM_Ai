{
  "build": {
    "builder": "RAILPACK",
    "root": "server",
    "env": {
      "PYTHON_VERSION": "3.11.9"
    }
  },
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT"
  }
}
